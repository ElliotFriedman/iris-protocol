# Demo Script — The Synthesis 2026

**Duration:** 2 minutes
**Format:** Live demo with voiceover

## Setup
- Dashboard open at [URL]
- Testnet funded with test ETH
- Two browser tabs ready: Dashboard + Block Explorer

## Script

### 0:00–0:10 — Hook
"Every AI agent that spends crypto today has a skeleton key to your wallet. Iris Protocol fixes that."

### 0:10–0:30 — Create Agent Wallet
- Click "Create Agent Wallet"
- Show: deterministic address generated via CREATE2
- "One click. The agent has an onchain identity registered via ERC-8004."

### 0:30–0:50 — Configure Trust Tier
- Select Tier 1 (Supervised)
- Show the Iris aperture narrowing
- "Tier 1: $100/day spending cap, approved contracts only, minimum reputation score of 50. All enforced by smart contracts — not a company server."

### 0:50–1:10 — Agent Transacts Successfully
- Trigger a small transaction within bounds
- Show it go through
- "The agent bought compute within its budget. The delegation was valid, all 4 caveat enforcers passed."

### 1:10–1:30 — Agent Blocked
- Trigger a transaction that exceeds the spending cap
- Show the revert
- "The agent tried to exceed its daily limit. The SpendingCapEnforcer blocked it onchain. No human intervention needed."

### 1:30–1:50 — Reputation Upgrade
- Show reputation score increasing after successful transactions
- Upgrade to Tier 2
- "As the agent proves itself, its reputation grows. Now it qualifies for Tier 2 — broader permissions, higher caps, still with guardrails."

### 1:50–2:00 — Close
"Iris Protocol: trustless agent wallets where every permission lives onchain. Agents earn trust — they don't assume it."

## Fallback
If live demo fails, switch to pre-recorded video at [URL].

## Key Points to Hit
- "Trustless" (no custodian)
- "Onchain" (not a policy engine)
- "Progressive trust" (earned, not assumed)
- "Open standards" (ERC-7710, ERC-8004)
