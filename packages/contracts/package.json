{
  "name": "@iris-protocol/contracts",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "deploy:local": "forge script script/DeployLocal.s.sol --fork-url http://127.0.0.1:8545 --broadcast",
    "deploy:base-sepolia": "forge script script/Deploy.s.sol --rpc-url $BASE_SEPOLIA_RPC_URL --broadcast --verify"
  }
}
